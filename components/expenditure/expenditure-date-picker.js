'use client';

import DatePicker from '@/components/date-picker';

const ExpenditureDatePicker = () => {
	return <DatePicker route='/expenditure' />;
};

export default ExpenditureDatePicker;
