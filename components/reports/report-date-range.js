'use client';

import DateRangePicker from '@/components/date-range-picker';

const ReportDateRange = () => {
	return <DateRangePicker route='/reports' />;
};

export default ReportDateRange;
